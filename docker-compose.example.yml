services:
  paperclip-mail-agent:
    image: ghcr.io/your-org/paperclip-mail-agent:latest
    container_name: paperclip-mail-agent
    env_file:
      - .env
    ports:
      - "8088:8088"
    restart: unless-stopped
